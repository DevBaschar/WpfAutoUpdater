
<Window
    x:Class="WpfAutoUpdater.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:WpfAutoUpdater.ViewModels"
    Title="Wpf Auto Updater"
    Width="520"
    Height="280"
    mc:Ignorable="d">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel
            Grid.Row="0"
            Margin="0,0,0,12"
            Orientation="Horizontal">
            <TextBlock FontWeight="SemiBold" Text="Current version:" />
            <TextBlock Text=" " />
            <TextBlock Text="{Binding CurrentVersion}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,12"
            Orientation="Horizontal">
            <TextBlock FontWeight="SemiBold" Text="Latest version:" />
            <TextBlock Text=" " />
            <TextBlock Text="{Binding LatestVersion}" />
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Margin="0,0,0,16"
            Orientation="Horizontal">
            <Button
                Width="160"
                Command="{Binding CheckForUpdateCommand}"
                Content="Check for Updates" />
            <Button
                Width="160"
                Command="{Binding DownloadAndInstallCommand}"
                Content="Download and Install"
                IsEnabled="{Binding IsUpdateAvailable}" />
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" />
            <ProgressBar
                Height="16"
                Maximum="100"
                Minimum="0"
                Value="{Binding ProgressValue}" />
            <TextBlock Text="{Binding ProgressText}" />
        </StackPanel>
    </Grid>
</Window>
