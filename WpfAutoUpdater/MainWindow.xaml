
<Window x:Class="WpfAutoUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WpfAutoUpdater.ViewModels"
        mc:Ignorable="d"
        Title="Wpf Auto Updater" Height="280" Width="520">
  <Window.DataContext>
    <vm:MainViewModel />
  </Window.DataContext>
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0 0 0 12">
      <TextBlock Text="Current version:" FontWeight="SemiBold"/>
      <TextBlock Text=" " />
      <TextBlock Text="{Binding CurrentVersion}" />
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0 0 0 12">
      <TextBlock Text="Latest version:" FontWeight="SemiBold"/>
      <TextBlock Text=" " />
      <TextBlock Text="{Binding LatestVersion}" />
    </StackPanel>

    <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0 0 0 16" Spacing="8">
      <Button Content="Check for Updates" Width="160" Command="{Binding CheckForUpdateCommand}"/>
      <Button Content="Download && Install" Width="160" Command="{Binding DownloadAndInstallCommand}" IsEnabled="{Binding IsUpdateAvailable}"/>
    </StackPanel>

    <StackPanel Grid.Row="3" Spacing="6">
      <TextBlock Text="{Binding Status}" TextWrapping="Wrap"/>
      <ProgressBar Minimum="0" Maximum="100" Height="16" Value="{Binding ProgressValue}"/>
      <TextBlock Text="{Binding ProgressText}" />
    </StackPanel>
  </Grid>
</Window>
